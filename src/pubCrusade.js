// only required for dev
// in prod, foundry loads moduleName.js, which is compiled by vite/rollup
// in dev, foundry loads moduleName.js, this file, which loads moduleName.ts

// some of your dependencies might need this
import "./pubCrusade.ts";
